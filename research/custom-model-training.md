# BoltHands Custom Model Training: Qwen3.5-9B Fine-Tuning with Unsloth

**Goal:** Fine-tune Qwen3.5-9B into an agentic coding specialist — tool calling, file operations, bash execution, error-driven iteration, and research-saving behaviors.

**Hardware:** RTX 3090 (24GB VRAM), 64GB DDR4, Unsloth Studio at `~/ai-drive/ai-suite/unsloth-studio/`

---

## 1. Training Datasets for Agentic Tool Use

### 1.1 Tier 1: Agent Trajectory Datasets (Most Relevant)

#### Nebius SWE-rebench OpenHands Trajectories
- **URL:** https://huggingface.co/datasets/nebius/SWE-rebench-openhands-trajectories
- **Size:** 67,074 agent trajectories across 1,823 Python repos
- **Format:** Multi-turn conversations with roles: system, user, assistant, tool. Assistant messages contain structured function calls (bash, file edit, etc.)
- **License:** CC-BY-4.0
- **Why it matters:** These are real agent-environment interaction traces — the model sees a task, calls bash, reads output, edits files, iterates on errors. This is exactly the BoltHands workflow.
- **Quality filter:** 32,161 successful trajectories out of 67,074 total. Use resolved=True to filter for successes only.
- **Load:**
  ```python
  from datasets import load_dataset
  ds = load_dataset("nebius/SWE-rebench-openhands-trajectories", split="train")
  # Filter for successful trajectories only
  ds_success = ds.filter(lambda x: x["resolved"])
  ```

#### SWE-Gym Dataset
- **URL:** https://github.com/SWE-Gym/SWE-Gym
- **Paper:** https://arxiv.org/abs/2412.21139 (ICML 2025)
- **Size:** 2,438 real Python task instances with executable environments and unit tests
- **Key result:** Fine-tuning on just 491 successful trajectories gave +14% absolute gain on SWE-Bench Verified
- **Training config from paper:** torchtune, lr=1e-4, max 5 epochs, global batch size 8, max context 32768
- **Why it matters:** Proven to dramatically improve agent coding ability with a small dataset

#### Qwen3-Coder Agent Trajectories (via Nebius RFT)
- **Context:** Qwen3-Coder was trained using 67K OpenHands trajectories with Rejection Filtering Training (RFT)
- **RFT Models released:**
  - 30B fine-tuned from Qwen3-30B: 50.3% Pass@1 on SWE-bench Verified
  - 235B fine-tuned from Qwen3-235B: 61.7% Pass@1
- **Collection:** https://huggingface.co/collections/nebius/openhands-trajectories
- **Approach:** Download trajectories, filter to successful ones, use for SFT on Qwen3.5-9B

### 1.2 Tier 2: Function Calling / Tool Use Datasets

#### Salesforce xLAM Function Calling 60K
- **URL:** https://huggingface.co/datasets/Salesforce/xlam-function-calling-60k
- **Size:** 60,000 examples, 3,673 APIs across 21 categories
- **Quality:** 95%+ correct rate in human eval (verified via format checking, execution, semantic verification)
- **Format:** JSON with query, tools (with descriptions/parameters), and expected function call answers
- **Generated by:** DeepSeek-V2-Chat and Mixtral-8x22B-Instruct
- **License:** CC-BY-NC-4.0 (check for commercial restrictions)

#### NousResearch Hermes Function Calling V1
- **URL:** https://huggingface.co/datasets/NousResearch/hermes-function-calling-v1
- **Format:** ShareGPT structure with `<tool_call>` / `<tool_response>` tags
- **Contents:** Single and multi-turn function calling, JSON-mode, agentic JSON-mode, structured extraction
- **Why it matters:** This is the exact dataset that gave Hermes 2 Pro its tool-calling abilities. Proven to work.
- **Example format:**
  ```json
  {
    "conversations": [
      {"from": "system", "value": "You have access to these tools: ..."},
      {"from": "human", "value": "What's the weather in Paris?"},
      {"from": "gpt", "value": "<tool_call>{\"name\": \"get_weather\", \"arguments\": {\"city\": \"Paris\"}}</tool_call>"},
      {"from": "tool", "value": "<tool_response>{\"temperature\": 22}</tool_response>"},
      {"from": "gpt", "value": "The weather in Paris is 22 degrees."}
    ]
  }
  ```

#### Glaive Function Calling V2
- **URL:** https://huggingface.co/datasets/glaiveai/glaive-function-calling-v2
- **Size:** 112,960 examples (271 MB)
- **License:** Apache 2.0
- **Format:** Conversation pairs with function definitions and calls
- **Variants:** ShareGPT-formatted versions available:
  - `lilacai/glaive-function-calling-v2-sharegpt`
  - `hiyouga/glaive-function-calling-v2-sharegpt`

#### Hypervariance Function Calling ShareGPT
- **URL:** https://huggingface.co/datasets/hypervariance/function-calling-sharegpt
- **Size:** 86,864 examples
- **Format:** ShareGPT with function calling integrated into conversations

### 1.3 Tier 3: Benchmark / Specialized Datasets

#### BFCL (Berkeley Function Calling Leaderboard)
- **URL:** https://huggingface.co/datasets/gorilla-llm/Berkeley-Function-Calling-Leaderboard
- **GitHub:** https://github.com/ShishirPatil/gorilla/tree/main/berkeley-function-call-leaderboard
- **Size:** ~2,000 question-function-answer pairs
- **Categories:** Simple (258), Multiple (1037), Parallel (16), Parallel Multiple (24)
- **Languages:** Python, Java, JavaScript, REST API
- **License:** Apache 2.0
- **Best use:** Evaluation benchmark, not primary training data (too small)

#### Gorilla API Dataset
- **URL:** https://github.com/ShishirPatil/gorilla
- **Size:** 14,189 instruction-API pairs
- **Focus:** API call generation with reduced hallucination
- **Specialty:** Retrieval-aware training — adapts to API documentation changes

#### ToolBench
- **URL:** https://github.com/OpenBMB/ToolBench (ICLR 2024 Spotlight)
- **HuggingFace:** https://huggingface.co/datasets/Maurus/ToolBench
- **Size:** 16,464 real-world APIs from RapidAPI Hub
- **Format:** Instruction-tuning SFT data with tool-use conversations
- **Model:** ToolLLaMA (fine-tuned on ToolBench)

#### ToolACE
- **URL:** https://huggingface.co/Team-ACE/ToolACE-8B
- **Specialized:** Tool-augmented code execution

### 1.4 Recommended Dataset Mix for BoltHands

| Dataset | Examples | Purpose | Priority |
|---------|----------|---------|----------|
| Nebius OpenHands Trajectories (filtered) | ~32K | Core agentic coding behavior | CRITICAL |
| NousResearch Hermes Function Calling | ~10K | Structured tool calling | HIGH |
| Salesforce xLAM 60K | 60K | Broad function calling | MEDIUM |
| Custom BoltHands examples (see Section 3) | 500-2000 | BoltHands-specific behaviors | HIGH |
| Glaive Function Calling V2 (subset) | ~10K | Additional diversity | LOW |

**Total target:** ~50K-80K examples after filtering and deduplication.

---

## 2. QLoRA Training Plan for RTX 3090

### 2.1 Model Loading

```python
from unsloth import FastLanguageModel
import torch

max_seq_length = 8192  # Can go up to 32768 but uses more VRAM
dtype = None  # Auto-detect (will use bf16 on RTX 3090 / Ampere)

model, tokenizer = FastLanguageModel.from_pretrained(
    model_name = "unsloth/Qwen3.5-9B",  # Use Unsloth's optimized version
    max_seq_length = max_seq_length,
    dtype = dtype,
    load_in_4bit = True,  # QLoRA: 4-bit quantized base model
)
```

### 2.2 LoRA Configuration

```python
model = FastLanguageModel.get_peft_model(
    model,
    r = 64,                    # LoRA rank — 64 recommended for tool-calling tasks
    target_modules = [
        "q_proj",              # Query projection (attention)
        "k_proj",              # Key projection (attention)
        "v_proj",              # Value projection (attention)
        "o_proj",              # Output projection (attention)
        "gate_proj",           # MLP gate projection
        "up_proj",             # MLP up projection
        "down_proj",           # MLP down projection
    ],
    lora_alpha = 64,           # Alpha = r is standard; alpha = 2*r for more aggressive learning
    lora_dropout = 0,          # Unsloth optimized — dropout=0 is fastest
    bias = "none",             # No bias training
    use_gradient_checkpointing = "unsloth",  # 30% less VRAM
    random_state = 3407,
    max_seq_length = max_seq_length,
    use_rslora = True,         # Rank-stabilized LoRA — better for higher ranks
)
```

### 2.3 Why These Settings

**LoRA Rank (r=64):**
- r=16 is fine for style/personality fine-tuning
- r=32-64 is better for learning new capabilities (tool calling, structured output)
- r=128 would be overkill and risks overfitting on smaller datasets
- For teaching a model entirely new behaviors (agentic patterns), higher rank captures more information

**Target Modules — ALL attention + MLP layers:**
- `q_proj, k_proj, v_proj, o_proj`: Attention layers control how the model attends to tool definitions, function signatures, and conversation context
- `gate_proj, up_proj, down_proj`: MLP layers control the model's "reasoning" — critical for deciding WHEN to call tools and WHAT arguments to use
- Targeting all 7 modules costs more VRAM but gives significantly better results for capability training

**Alpha = Rank:**
- `lora_alpha = r` gives a scaling factor of 1.0 (alpha/r)
- This is the conservative, stable choice
- If results are underwhelming, try `lora_alpha = 2 * r` for stronger adaptation

### 2.4 Training Configuration

```python
from trl import SFTTrainer, SFTConfig

trainer = SFTTrainer(
    model = model,
    tokenizer = tokenizer,
    train_dataset = dataset,
    eval_dataset = eval_dataset,       # Hold out 5-10% for eval
    args = SFTConfig(
        # Core training params
        per_device_train_batch_size = 2,       # 2 fits on 24GB with QLoRA + grad checkpointing
        gradient_accumulation_steps = 8,        # Effective batch size = 16
        num_train_epochs = 2,                   # 2-3 epochs max to avoid overfitting
        learning_rate = 2e-4,                   # Unsloth default, good starting point
        lr_scheduler_type = "cosine",           # Cosine decay with warmup
        warmup_ratio = 0.05,                    # 5% warmup

        # Precision & optimization
        fp16 = not torch.cuda.is_bf16_supported(),
        bf16 = torch.cuda.is_bf16_supported(),  # True for RTX 3090
        optim = "adamw_8bit",                    # 8-bit Adam saves VRAM

        # Sequence length
        max_seq_length = max_seq_length,
        dataset_text_field = "text",             # Or use formatting_func

        # Logging & saving
        logging_steps = 10,
        save_strategy = "steps",
        save_steps = 500,
        save_total_limit = 3,
        output_dir = "bolthands-qwen35-9b-lora",

        # Eval
        eval_strategy = "steps",
        eval_steps = 500,

        # Performance
        dataloader_num_workers = 4,
        seed = 3407,
    ),
)
```

### 2.5 VRAM Budget on RTX 3090 (24GB)

| Component | VRAM |
|-----------|------|
| Qwen3.5-9B in 4-bit (QLoRA) | ~6 GB |
| LoRA adapters (r=64, all modules) | ~1 GB |
| Optimizer states (8-bit Adam) | ~2 GB |
| Activations (gradient checkpointing) | ~6-8 GB |
| Batch (batch_size=2, seq_len=8192) | ~4-6 GB |
| **Total estimated** | **~19-23 GB** |

This fits within 24GB. If you hit OOM:
1. Reduce `per_device_train_batch_size` to 1 (increase `gradient_accumulation_steps` to 16)
2. Reduce `max_seq_length` to 4096
3. Reduce `r` to 32

### 2.6 Training Time Estimates

| Dataset Size | Epochs | Estimated Time (RTX 3090) |
|-------------|--------|--------------------------|
| 10K examples | 2 | ~2-4 hours |
| 30K examples | 2 | ~6-12 hours |
| 50K examples | 2 | ~10-20 hours |
| 80K examples | 2 | ~16-32 hours |

Unsloth claims 2x speedup over standard HuggingFace training. These estimates assume `max_seq_length=8192` and `batch_size=2`.

### 2.7 Data Formatting for Qwen3.5

Qwen3.5 uses the ChatML format with XML-style tool calling. Training data should follow this template:

```
<|im_start|>system
You are BoltHands, an agentic coding assistant. You have access to the following tools:

<tools>
[{"type": "function", "function": {"name": "bash", "description": "Execute a bash command", "parameters": {"type": "object", "properties": {"command": {"type": "string"}}}}}]
[{"type": "function", "function": {"name": "write_file", "description": "Write content to a file", "parameters": {"type": "object", "properties": {"path": {"type": "string"}, "content": {"type": "string"}}}}}]
[{"type": "function", "function": {"name": "read_file", "description": "Read a file's contents", "parameters": {"type": "object", "properties": {"path": {"type": "string"}}}}}]
</tools>

When you need to use a tool, respond with:
<tool_call>
{"name": "tool_name", "arguments": {"arg1": "value1"}}
</tool_call>
<|im_end|>
<|im_start|>user
Fix the TypeError in src/parser.py
<|im_end|>
<|im_start|>assistant
I'll start by reading the file to understand the error.

<tool_call>
{"name": "read_file", "arguments": {"path": "src/parser.py"}}
</tool_call>
<|im_end|>
<|im_start|>tool
<tool_response>
{"content": "def parse(data):\n    return data.split(',')\n"}
</tool_response>
<|im_end|>
<|im_start|>assistant
I see the issue — `data` could be None. Let me fix it and run the tests.

<tool_call>
{"name": "write_file", "arguments": {"path": "src/parser.py", "content": "def parse(data):\n    if data is None:\n        return []\n    return data.split(',')\n"}}
</tool_call>
<|im_end|>
<|im_start|>tool
<tool_response>
{"status": "success"}
</tool_response>
<|im_end|>
<|im_start|>assistant
<tool_call>
{"name": "bash", "arguments": {"command": "python -m pytest tests/test_parser.py -v"}}
</tool_call>
<|im_end|>
<|im_start|>tool
<tool_response>
{"output": "tests/test_parser.py::test_parse_none PASSED\ntests/test_parser.py::test_parse_csv PASSED\n2 passed in 0.01s"}
</tool_response>
<|im_end|>
<|im_start|>assistant
Fixed the TypeError in `src/parser.py`. The issue was that `data.split(',')` would fail when `data` is `None`. Added a None check that returns an empty list. All tests pass.
<|im_end|>
```

### 2.8 Formatting Function for Training

```python
def format_bolthands_example(example):
    """Convert a training example to Qwen3.5 ChatML format."""
    messages = example["messages"]  # List of {role, content} dicts
    formatted = ""
    for msg in messages:
        role = msg["role"]
        content = msg["content"]
        formatted += f"<|im_start|>{role}\n{content}\n<|im_end|>\n"
    return {"text": formatted}

# Apply formatting
dataset = dataset.map(format_bolthands_example)
```

---

## 3. Custom Training Data Creation

### 3.1 Converting OpenHands Sessions to Training Pairs

The Agent Data Protocol (ADP) provides a standardized schema for converting diverse agent trajectories into SFT format. Here's how to convert OpenHands sessions:

```python
import json
from datasets import load_dataset

def convert_openhands_to_chatml(trajectory):
    """
    Convert an OpenHands trajectory to Qwen3.5 ChatML format
    for BoltHands fine-tuning.
    """
    messages = []

    # System prompt with BoltHands tool definitions
    system_prompt = """You are BoltHands, an expert agentic coding assistant.

CORE BEHAVIORS:
1. Always read files before modifying them
2. Save research and plans to files before starting work
3. When errors occur, search for the error message to understand the cause
4. Write a PLAN.md before starting complex tasks
5. Test changes after making them
6. Iterate on errors until resolved — never give up after first failure

<tools>
[{"type": "function", "function": {"name": "bash", "description": "Execute a bash command and return output", "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "The bash command to execute"}}, "required": ["command"]}}},
{"type": "function", "function": {"name": "write_file", "description": "Write or overwrite a file", "parameters": {"type": "object", "properties": {"path": {"type": "string"}, "content": {"type": "string"}}, "required": ["path", "content"]}}},
{"type": "function", "function": {"name": "read_file", "description": "Read a file's contents", "parameters": {"type": "object", "properties": {"path": {"type": "string"}}, "required": ["path"]}}},
{"type": "function", "function": {"name": "search_files", "description": "Search for patterns in files", "parameters": {"type": "object", "properties": {"pattern": {"type": "string"}, "path": {"type": "string"}}, "required": ["pattern"]}}}]
</tools>"""

    messages.append({"role": "system", "content": system_prompt})

    # Convert each step in the trajectory
    for step in trajectory["messages"]:
        role = step.get("role", "")
        content = step.get("content", "")

        if role == "user":
            messages.append({"role": "user", "content": content})
        elif role == "assistant":
            # Convert OpenHands action format to Qwen3.5 tool_call format
            if "function_call" in step or "tool_calls" in step:
                tool_calls = step.get("tool_calls", [step.get("function_call")])
                tool_text = content or ""
                for tc in tool_calls:
                    func = tc.get("function", tc)
                    tool_text += f'\n<tool_call>\n{{"name": "{func["name"]}", "arguments": {func["arguments"]}}}\n</tool_call>'
                messages.append({"role": "assistant", "content": tool_text.strip()})
            else:
                messages.append({"role": "assistant", "content": content})
        elif role == "tool":
            messages.append({"role": "tool", "content": f"<tool_response>\n{content}\n</tool_response>"})

    return messages


# Load and convert
ds = load_dataset("nebius/SWE-rebench-openhands-trajectories", split="train")
ds_success = ds.filter(lambda x: x.get("resolved", False))

converted = []
for example in ds_success:
    messages = convert_openhands_to_chatml(example)
    converted.append({"messages": messages})
```

### 3.2 Synthetic Data Generation for BoltHands-Specific Behaviors

Use Claude (or GPT-4) to generate training examples that reinforce BoltHands behaviors. Here are templates for each key behavior:

#### Behavior 1: "Save research to file before proceeding"

```
Prompt to Claude/GPT-4:
Generate a multi-turn agent conversation where:
1. User asks to research [topic]
2. Agent uses bash to search for information
3. Agent EXPLICITLY writes findings to a research file BEFORE answering
4. Agent then proceeds with the task using the saved research

Generate 50 diverse examples covering different topics.
Format each as a list of {role, content} messages with tool_call XML tags.
```

Example output:
```json
{
  "messages": [
    {"role": "system", "content": "[BoltHands system prompt with tools]"},
    {"role": "user", "content": "Research how to implement WebSocket authentication in FastAPI"},
    {"role": "assistant", "content": "I'll research WebSocket authentication in FastAPI and save my findings.\n\n<tool_call>\n{\"name\": \"bash\", \"arguments\": {\"command\": \"find . -name '*.py' | head -20 && pip show fastapi 2>/dev/null | grep Version\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\nVersion: 0.109.0\n</tool_response>"},
    {"role": "assistant", "content": "Let me save my research findings first.\n\n<tool_call>\n{\"name\": \"write_file\", \"arguments\": {\"path\": \"research/websocket-auth.md\", \"content\": \"# WebSocket Authentication in FastAPI\\n\\n## Approaches\\n1. Token in query params...\\n2. First message auth...\\n3. Cookie-based...\\n\\n## Recommended: Token in query params\\n...\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\n{\"status\": \"success\"}\n</tool_response>"},
    {"role": "assistant", "content": "I've saved my research to `research/websocket-auth.md`. Now let me implement the solution..."}
  ]
}
```

#### Behavior 2: "When an error occurs, search for the error message"

```json
{
  "messages": [
    {"role": "system", "content": "[BoltHands system prompt with tools]"},
    {"role": "user", "content": "Run the test suite"},
    {"role": "assistant", "content": "<tool_call>\n{\"name\": \"bash\", \"arguments\": {\"command\": \"python -m pytest tests/ -v\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\nFAILED tests/test_api.py::test_create_user - AttributeError: 'NoneType' object has no attribute 'id'\n</tool_response>"},
    {"role": "assistant", "content": "Test failed with `AttributeError: 'NoneType' object has no attribute 'id'`. Let me search for where this occurs.\n\n<tool_call>\n{\"name\": \"search_files\", \"arguments\": {\"pattern\": \"\\.id\", \"path\": \"src/api/\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\nsrc/api/users.py:45:    return user.id\n</tool_response>"},
    {"role": "assistant", "content": "Found it. Let me read the context around that line.\n\n<tool_call>\n{\"name\": \"read_file\", \"arguments\": {\"path\": \"src/api/users.py\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\ndef create_user(name):\n    user = db.query(User).filter(User.name == name).first()\n    return user.id\n</tool_response>"},
    {"role": "assistant", "content": "The issue is that `db.query(...).first()` returns `None` when no user is found, but the code assumes it always exists. Let me fix this.\n\n<tool_call>\n{\"name\": \"write_file\", \"arguments\": {\"path\": \"src/api/users.py\", \"content\": \"def create_user(name):\\n    user = db.query(User).filter(User.name == name).first()\\n    if user is None:\\n        user = User(name=name)\\n        db.add(user)\\n        db.commit()\\n        db.refresh(user)\\n    return user.id\\n\"}}\n</tool_call>"}
  ]
}
```

#### Behavior 3: "Write plan to PLAN.md before starting"

```json
{
  "messages": [
    {"role": "system", "content": "[BoltHands system prompt with tools]"},
    {"role": "user", "content": "Add user authentication to the API"},
    {"role": "assistant", "content": "This is a complex feature. Let me plan it out first.\n\n<tool_call>\n{\"name\": \"read_file\", \"arguments\": {\"path\": \"src/main.py\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\n[file contents]\n</tool_response>"},
    {"role": "assistant", "content": "<tool_call>\n{\"name\": \"write_file\", \"arguments\": {\"path\": \"PLAN.md\", \"content\": \"# Authentication Implementation Plan\\n\\n## Steps\\n1. Install dependencies (passlib, python-jose, bcrypt)\\n2. Create User model with hashed password field\\n3. Add /register and /login endpoints\\n4. Implement JWT token generation\\n5. Add auth middleware\\n6. Protect existing endpoints\\n7. Write tests\\n\\n## Files to modify\\n- src/models.py (add User)\\n- src/auth.py (new — JWT logic)\\n- src/routes/auth.py (new — login/register)\\n- src/main.py (add middleware)\\n- requirements.txt (add deps)\\n- tests/test_auth.py (new)\\n\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\n{\"status\": \"success\"}\n</tool_response>"},
    {"role": "assistant", "content": "Plan saved to `PLAN.md`. Starting with step 1: installing dependencies.\n\n<tool_call>\n{\"name\": \"bash\", \"arguments\": {\"command\": \"pip install passlib[bcrypt] python-jose[cryptography]\"}}\n</tool_call>"}
  ]
}
```

#### Behavior 4: "Read files instead of relying on conversation history"

```json
{
  "messages": [
    {"role": "system", "content": "[BoltHands system prompt with tools]"},
    {"role": "user", "content": "Now update the config to use port 8080 instead"},
    {"role": "assistant", "content": "Let me read the current config file to make sure I have the latest version.\n\n<tool_call>\n{\"name\": \"read_file\", \"arguments\": {\"path\": \"config.yaml\"}}\n</tool_call>"},
    {"role": "tool", "content": "<tool_response>\nserver:\n  port: 3000\n  host: 0.0.0.0\ndatabase:\n  url: sqlite:///app.db\n</tool_response>"},
    {"role": "assistant", "content": "<tool_call>\n{\"name\": \"write_file\", \"arguments\": {\"path\": \"config.yaml\", \"content\": \"server:\\n  port: 8080\\n  host: 0.0.0.0\\ndatabase:\\n  url: sqlite:///app.db\\n\"}}\n</tool_call>"}
  ]
}
```

### 3.3 Synthetic Data Generation Script

```python
"""
generate_bolthands_training_data.py
Use a strong LLM (Claude, GPT-4) to generate synthetic agentic training examples.
"""

import json
import random

BEHAVIORS = [
    {
        "name": "save_research_first",
        "instruction": "Generate a conversation where the agent researches a topic and saves findings to a file BEFORE proceeding with the task.",
        "count": 200,
    },
    {
        "name": "search_on_error",
        "instruction": "Generate a conversation where the agent encounters an error, searches for the error message in the codebase, finds the root cause, and fixes it.",
        "count": 300,
    },
    {
        "name": "plan_before_coding",
        "instruction": "Generate a conversation where the agent writes a PLAN.md file before starting a complex multi-step coding task.",
        "count": 200,
    },
    {
        "name": "read_before_edit",
        "instruction": "Generate a conversation where the agent always reads a file before modifying it, even if the file was discussed earlier in the conversation.",
        "count": 200,
    },
    {
        "name": "iterate_on_failure",
        "instruction": "Generate a conversation where the agent's first attempt fails, it analyzes the error, tries a different approach, and eventually succeeds after 2-3 iterations.",
        "count": 300,
    },
    {
        "name": "bash_for_exploration",
        "instruction": "Generate a conversation where the agent uses bash commands (ls, find, grep, cat, git log) to explore a codebase before making changes.",
        "count": 200,
    },
    {
        "name": "test_after_changes",
        "instruction": "Generate a conversation where the agent makes code changes and immediately runs tests to verify them, fixing any failures.",
        "count": 200,
    },
]

PROGRAMMING_TASKS = [
    "Fix a bug in a Python web API",
    "Add a new CLI command to a Node.js tool",
    "Refactor a Rust module for better error handling",
    "Write unit tests for a database layer",
    "Implement pagination for a REST endpoint",
    "Fix a race condition in async code",
    "Add logging to a microservice",
    "Optimize a slow database query",
    "Add input validation to form handlers",
    "Fix a memory leak in a long-running process",
    "Implement rate limiting middleware",
    "Add WebSocket support to an existing HTTP server",
    "Fix broken CI/CD pipeline configuration",
    "Add environment variable configuration",
    "Implement a retry mechanism with exponential backoff",
]

# Use this prompt template with Claude API or OpenAI API to generate examples
GENERATION_PROMPT = """You are generating training data for an AI coding agent called BoltHands.

TASK: {instruction}

PROGRAMMING CONTEXT: {task}

Generate a realistic multi-turn conversation between a user and the BoltHands agent.
The agent has these tools: bash, write_file, read_file, search_files.

Requirements:
- Use realistic file paths, code, and error messages
- The agent should think out loud briefly before each tool call
- Tool calls must use this exact format: <tool_call>\\n{{"name": "tool_name", "arguments": {{...}}}}\\n</tool_call>
- Tool responses use: <tool_response>\\n...\\n</tool_response>
- Include at least 3 tool calls in the conversation
- Make the code realistic and varied (Python, JS, Rust, Go, etc.)

Output format: JSON with a "messages" array of {{"role": "...", "content": "..."}} objects.
Roles: system, user, assistant, tool
"""
```

### 3.4 Using Unsloth Studio's Data Recipes

Unsloth Studio includes Data Recipes (powered by NVIDIA DataDesigner) for visual dataset creation:

1. **Open Unsloth Studio** at `~/ai-drive/ai-suite/unsloth-studio/`
2. **Navigate to Data Recipes** tab
3. **Create a new recipe** with the following workflow:
   - **Source Node:** Upload your existing BoltHands conversation logs (JSON/JSONL)
   - **Transform Node:** Apply formatting to match Qwen3.5 ChatML template
   - **Generate Node:** Use a local LLM (Qwen3.5-27B or Claude API) to generate synthetic variations
   - **Filter Node:** Remove low-quality examples (too short, missing tool calls, etc.)
   - **Output Node:** Export as HuggingFace Dataset format
4. **Use a strong generator model** — Qwen3.5-27B or an API model (Claude/GPT-4) for highest quality
5. **Run preview** on 10-20 examples, inspect quality, adjust prompts
6. **Run full generation** once quality is confirmed

### 3.5 Data Quality Guidelines

- **Minimum conversation length:** 4 turns (user + at least 2 agent actions + resolution)
- **Required elements:** At least one tool call per example
- **Diversity:** Mix of languages (Python 50%, JS 20%, Rust/Go/other 30%)
- **Error handling:** At least 30% of examples should include error recovery
- **File operations:** At least 40% should include read_file before write_file
- **No hallucinated outputs:** Tool responses should be realistic (real error messages, real command output)

---

## 4. Evaluation

### 4.1 BFCL Benchmark (Automated)

Use the Berkeley Function Calling Leaderboard to measure general function-calling ability:

```bash
# Clone BFCL
git clone https://github.com/ShishirPatil/gorilla.git
cd gorilla/berkeley-function-call-leaderboard

# Run eval against your fine-tuned model (via llama.cpp server)
# The BFCL supports custom model endpoints
python eval_runner.py \
    --model custom \
    --api-base http://localhost:8080/v1 \
    --model-name bolthands-qwen35-9b
```

### 4.2 Custom BoltHands Evaluation Suite

Create a set of 50-100 test scenarios that measure BoltHands-specific behaviors:

```python
"""
eval_bolthands.py — Custom evaluation for BoltHands behaviors
"""

EVAL_SCENARIOS = [
    {
        "name": "saves_research_before_answering",
        "prompt": "Research the best Python async HTTP libraries and recommend one for our project",
        "pass_criteria": [
            "calls write_file with a research/notes file",
            "write_file happens BEFORE the final recommendation",
        ],
    },
    {
        "name": "reads_file_before_editing",
        "prompt": "Change the database port in config.yaml from 5432 to 5433",
        "pass_criteria": [
            "calls read_file on config.yaml BEFORE write_file",
            "write_file preserves all existing config, only changes port",
        ],
    },
    {
        "name": "searches_on_error",
        "prompt": "Run the tests",
        "setup": "# Pre-configure a failing test",
        "pass_criteria": [
            "after test failure, searches for error message (grep/search_files)",
            "reads the failing source file",
            "fixes the issue",
            "re-runs tests to confirm fix",
        ],
    },
    {
        "name": "writes_plan_for_complex_task",
        "prompt": "Add OAuth2 login with Google to our Flask app",
        "pass_criteria": [
            "creates a PLAN.md or plan file before starting implementation",
            "plan includes numbered steps",
            "follows the plan sequentially",
        ],
    },
    {
        "name": "iterates_on_failure",
        "prompt": "Install and configure Redis for session storage",
        "setup": "# Redis is not installed, pip install will fail first",
        "pass_criteria": [
            "first attempt fails",
            "agent tries alternative approach",
            "eventually succeeds or clearly reports what's needed",
            "does NOT give up after first error",
        ],
    },
    {
        "name": "uses_bash_for_exploration",
        "prompt": "Familiarize yourself with this codebase and tell me what it does",
        "pass_criteria": [
            "uses ls/find to explore directory structure",
            "reads key files (README, main entry point, package.json/pyproject.toml)",
            "uses grep to find patterns",
            "provides structured summary",
        ],
    },
]

def evaluate_trajectory(scenario, trajectory):
    """
    Score a trajectory against pass criteria.
    Returns (pass_count, total_criteria, details)
    """
    results = []
    for criterion in scenario["pass_criteria"]:
        # Check if the trajectory satisfies this criterion
        # This can be done by:
        # 1. Simple string matching on tool calls
        # 2. LLM-as-judge (use Claude/GPT-4 to evaluate)
        # 3. Programmatic checks on tool call sequence
        passed = check_criterion(trajectory, criterion)
        results.append({"criterion": criterion, "passed": passed})

    pass_count = sum(1 for r in results if r["passed"])
    return pass_count, len(results), results
```

### 4.3 A/B Comparison Script

```python
"""
Compare base Qwen3.5-9B vs fine-tuned BoltHands model on the same tasks.
"""

import json
import requests

BASE_MODEL_URL = "http://localhost:8080/v1"      # Base Qwen3.5-9B
FINETUNED_URL = "http://localhost:8081/v1"        # BoltHands fine-tuned

TASKS = [
    "Fix the TypeError in src/utils.py",
    "Add rate limiting to the /api/users endpoint",
    "Research FastAPI middleware patterns and implement logging",
    "The CI pipeline is failing — investigate and fix",
    "Refactor the database module to use connection pooling",
]

def run_agent(url, task, max_turns=20):
    """Run agent loop and collect trajectory."""
    messages = [
        {"role": "system", "content": BOLTHANDS_SYSTEM_PROMPT},
        {"role": "user", "content": task},
    ]
    trajectory = []

    for _ in range(max_turns):
        response = requests.post(f"{url}/chat/completions", json={
            "model": "bolthands",
            "messages": messages,
            "temperature": 0.1,
        })
        assistant_msg = response.json()["choices"][0]["message"]
        trajectory.append(assistant_msg)
        messages.append(assistant_msg)

        # Check for tool calls and execute them
        if has_tool_call(assistant_msg):
            tool_result = execute_tool(assistant_msg)
            messages.append({"role": "tool", "content": tool_result})
            trajectory.append({"role": "tool", "content": tool_result})
        else:
            break  # Agent finished

    return trajectory

def compare():
    results = []
    for task in TASKS:
        base_traj = run_agent(BASE_MODEL_URL, task)
        ft_traj = run_agent(FINETUNED_URL, task)

        results.append({
            "task": task,
            "base_tool_calls": count_tool_calls(base_traj),
            "ft_tool_calls": count_tool_calls(ft_traj),
            "base_used_bash": has_bash_call(base_traj),
            "ft_used_bash": has_bash_call(ft_traj),
            "base_saved_files": has_file_write(base_traj),
            "ft_saved_files": has_file_write(ft_traj),
            "base_iterated_on_error": has_error_recovery(base_traj),
            "ft_iterated_on_error": has_error_recovery(ft_traj),
        })

    # Print comparison table
    for r in results:
        print(f"\nTask: {r['task']}")
        print(f"  Tool calls:    base={r['base_tool_calls']}, fine-tuned={r['ft_tool_calls']}")
        print(f"  Used bash:     base={r['base_used_bash']}, fine-tuned={r['ft_used_bash']}")
        print(f"  Saved files:   base={r['base_saved_files']}, fine-tuned={r['ft_saved_files']}")
        print(f"  Error recovery: base={r['base_iterated_on_error']}, fine-tuned={r['ft_iterated_on_error']}")
```

### 4.4 Metrics to Track

| Metric | How to Measure | Target |
|--------|---------------|--------|
| Tool call accuracy | BFCL benchmark | Top 50% of leaderboard |
| Tool usage rate | % of responses containing tool calls | >80% when task requires tools |
| Read-before-write | % of file edits preceded by read | >90% |
| Error recovery | % of errors followed by investigation | >85% |
| Plan creation | % of complex tasks with plan file | >70% |
| Research saving | % of research tasks saved to file | >80% |
| Task completion | End-to-end success on eval suite | >60% |

---

## 5. Export and Deployment

### 5.1 Export to GGUF via Unsloth

```python
# After training is complete:

# Option 1: Save merged model to GGUF (recommended for deployment)
model.save_pretrained_gguf(
    "bolthands-qwen35-9b-gguf",
    tokenizer,
    quantization_method = "q5_k_m"   # Best quality/size ratio for 9B
)

# Option 2: Multiple quantizations for testing
for quant in ["q4_k_m", "q5_k_m", "q8_0"]:
    model.save_pretrained_gguf(
        f"bolthands-qwen35-9b-{quant}",
        tokenizer,
        quantization_method = quant
    )

# Option 3: Save LoRA adapter separately (for stacking)
model.save_pretrained("bolthands-lora-adapter")  # Saves PEFT adapter
tokenizer.save_pretrained("bolthands-lora-adapter")
```

### 5.2 Quantization Options Comparison

| Quant | Size (9B) | Quality | Speed | Recommendation |
|-------|-----------|---------|-------|---------------|
| q4_k_m | ~5.5 GB | Good | Fastest | Quick testing, lower VRAM |
| q5_k_m | ~6.5 GB | Very Good | Fast | **Best balance — recommended** |
| q6_k | ~7.5 GB | Excellent | Moderate | If VRAM allows |
| q8_0 | ~9.5 GB | Near-lossless | Slower | Maximum quality |
| f16 | ~18 GB | Lossless | Slowest | Reference only |

**Recommendation for RTX 3090:** Use `q5_k_m` for deployment. It fits easily in VRAM alongside other services and preserves tool-calling accuracy well.

### 5.3 Merging LoRA Weights into Base Model

```python
# Method 1: Unsloth built-in merge
model.save_pretrained_merged(
    "bolthands-qwen35-9b-merged",
    tokenizer,
    save_method = "merged_16bit",  # Full precision merge
)

# Method 2: Then convert merged model to GGUF manually
# (if you need more control over the quantization process)
# ~/ai-drive/llama.cpp/build/bin/llama-quantize \
#     bolthands-qwen35-9b-merged/ggml-model-f16.gguf \
#     bolthands-qwen35-9b-q5_k_m.gguf q5_k_m
```

### 5.4 Deploy in Existing llama.cpp Router

```bash
# Start llama.cpp server with the fine-tuned model
~/ai-drive/llama.cpp/build/bin/llama-server \
    --model ~/ai-drive/ai-suite/models/bolthands-qwen35-9b-q5_k_m.gguf \
    --port 8080 \
    --ctx-size 8192 \
    --n-gpu-layers 99 \
    --flash-attn \
    --chat-template chatml
```

### 5.5 LoRA Adapter Stacking

Yes, you CAN stack multiple LoRA adapters with llama.cpp. This enables a modular approach:

**Base Model** + **Tool-Calling LoRA** + **BoltHands-Specific LoRA**

#### How it works in llama.cpp:

```bash
# Convert LoRA adapter to GGUF format
python ~/ai-drive/llama.cpp/convert_lora_to_gguf.py \
    --base unsloth/Qwen3.5-9B \
    --lora-path bolthands-lora-adapter/ \
    --outfile bolthands-lora.gguf

# Load base model with LoRA adapter(s)
~/ai-drive/llama.cpp/build/bin/llama-server \
    --model ~/ai-drive/ai-suite/models/qwen3.5-9b-q5_k_m.gguf \
    --lora bolthands-lora.gguf \
    --lora-scaled bolthands-lora.gguf 1.0 \
    --port 8080 \
    --ctx-size 8192 \
    --n-gpu-layers 99
```

#### Hot-swapping LoRA at runtime:

llama.cpp server supports hot-swapping LoRA adapters via the `/lora-adapters` API endpoint:

```bash
# Change active LoRA adapter without restarting
curl -X POST http://localhost:8080/lora-adapters \
    -H "Content-Type: application/json" \
    -d '[{"path": "bolthands-lora.gguf", "scale": 1.0}]'

# Disable LoRA (use base model)
curl -X POST http://localhost:8080/lora-adapters \
    -H "Content-Type: application/json" \
    -d '[]'

# Stack multiple LoRAs
curl -X POST http://localhost:8080/lora-adapters \
    -H "Content-Type: application/json" \
    -d '[
        {"path": "tool-calling-lora.gguf", "scale": 0.8},
        {"path": "bolthands-behavior-lora.gguf", "scale": 1.0}
    ]'
```

#### Per-request LoRA (advanced):

llama.cpp also supports per-request LoRA adapters, meaning different API calls can use different LoRA configurations without affecting other users.

#### Recommended Stacking Strategy for BoltHands:

1. **Train LoRA 1 — "Tool Calling Foundation"** (on Hermes + xLAM + Glaive data)
   - Teaches structured tool calling, function signatures, argument formatting
   - ~50K examples, r=32
2. **Train LoRA 2 — "Agentic Coding"** (on OpenHands trajectories + SWE-Gym)
   - Teaches multi-step coding workflows, error iteration, bash usage
   - ~30K examples, r=32
3. **Train LoRA 3 — "BoltHands Behaviors"** (on custom synthetic data)
   - Teaches plan-first, save-research, read-before-write
   - ~1-2K examples, r=16 (small but targeted)
4. **Stack all three at inference time** with different scales

Alternatively, merge LoRA 1 into the base model, then train LoRA 2 on the merged model, then train LoRA 3 on that — giving you a single merged model. This avoids stacking complexity but loses modularity.

---

## 6. The Vision: A Self-Improving Training Loop

### 6.1 Architecture Overview

```
                    +------------------+
                    |   User runs      |
                    |   BoltHands      |
                    +--------+---------+
                             |
                    Session logs saved
                             |
                    +--------v---------+
                    |  Session Logger  |
                    |  (JSON traces)   |
                    +--------+---------+
                             |
                    +--------v---------+
                    | Quality Filter   |
                    | (success/fail,   |
                    |  tool usage,     |
                    |  error recovery) |
                    +--------+---------+
                             |
                    +--------v---------+
                    | Format Converter |
                    | (→ ChatML SFT)   |
                    +--------+---------+
                             |
                    +--------v---------+
                    |  Training Data   |
                    |  Accumulator     |
                    +--------+---------+
                             |
                    (Weekly/Monthly trigger)
                             |
                    +--------v---------+
                    |  Unsloth QLoRA   |
                    |  Fine-tuning     |
                    +--------+---------+
                             |
                    +--------v---------+
                    |  Evaluation      |
                    |  (compare to     |
                    |   current model) |
                    +--------+---------+
                             |
                    (Only deploy if better)
                             |
                    +--------v---------+
                    |  Deploy new GGUF |
                    |  to llama.cpp    |
                    +--------+---------+
                             |
                    (Loop repeats)
```

### 6.2 Session Logger Implementation

```python
"""
bolthands_logger.py — Log all agent sessions for future training
"""

import json
import time
import os
from datetime import datetime
from pathlib import Path

LOG_DIR = Path("~/ai-drive/bolthands/training-logs/").expanduser()
LOG_DIR.mkdir(parents=True, exist_ok=True)

class SessionLogger:
    def __init__(self, task_description: str):
        self.session_id = f"{datetime.now().strftime('%Y%m%d_%H%M%S')}_{os.getpid()}"
        self.task = task_description
        self.messages = []
        self.metadata = {
            "session_id": self.session_id,
            "task": task_description,
            "start_time": time.time(),
            "model_version": "bolthands-v1",
            "tool_calls": 0,
            "errors_encountered": 0,
            "errors_recovered": 0,
            "files_read": 0,
            "files_written": 0,
            "bash_commands": 0,
            "success": None,  # Set at end
        }

    def log_message(self, role: str, content: str, tool_name: str = None):
        entry = {
            "role": role,
            "content": content,
            "timestamp": time.time(),
        }
        if tool_name:
            entry["tool_name"] = tool_name
            self.metadata["tool_calls"] += 1
            if tool_name == "bash":
                self.metadata["bash_commands"] += 1
            elif tool_name == "read_file":
                self.metadata["files_read"] += 1
            elif tool_name == "write_file":
                self.metadata["files_written"] += 1

        # Detect errors in tool responses
        if role == "tool" and any(kw in content.lower() for kw in
            ["error", "traceback", "failed", "exception", "errno"]):
            self.metadata["errors_encountered"] += 1

        self.messages.append(entry)

    def mark_success(self, success: bool):
        self.metadata["success"] = success
        self.metadata["end_time"] = time.time()
        self.metadata["duration_seconds"] = self.metadata["end_time"] - self.metadata["start_time"]
        self.metadata["total_turns"] = len(self.messages)

        # Calculate error recovery rate
        if self.metadata["errors_encountered"] > 0:
            # Count errors followed by more tool calls (= recovery attempts)
            for i, msg in enumerate(self.messages):
                if msg["role"] == "tool" and "error" in msg["content"].lower():
                    # Check if agent continued with more actions
                    remaining = self.messages[i+1:]
                    if any(m["role"] == "assistant" and "<tool_call>" in m.get("content", "")
                           for m in remaining):
                        self.metadata["errors_recovered"] += 1

    def save(self):
        log_file = LOG_DIR / f"{self.session_id}.json"
        with open(log_file, "w") as f:
            json.dump({
                "metadata": self.metadata,
                "messages": self.messages,
            }, f, indent=2)
        return log_file
```

### 6.3 Quality Filter & Training Data Converter

```python
"""
convert_logs_to_training.py — Convert session logs to training data
"""

import json
from pathlib import Path
from datasets import Dataset

LOG_DIR = Path("~/ai-drive/bolthands/training-logs/").expanduser()
OUTPUT_DIR = Path("~/ai-drive/bolthands/training-data/").expanduser()
OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

def quality_filter(session: dict) -> bool:
    """Only include high-quality sessions in training data."""
    meta = session["metadata"]

    # Must be a successful session
    if not meta.get("success"):
        return False

    # Must have used tools (not just conversation)
    if meta["tool_calls"] < 2:
        return False

    # Must have reasonable length (not too short, not runaway loops)
    if meta["total_turns"] < 4 or meta["total_turns"] > 100:
        return False

    # Bonus: sessions with error recovery are especially valuable
    # (but don't require it)

    return True

def convert_to_chatml(session: dict) -> dict:
    """Convert a session log to ChatML training format."""
    chatml_messages = []

    # Add system prompt
    chatml_messages.append({
        "role": "system",
        "content": BOLTHANDS_SYSTEM_PROMPT,
    })

    for msg in session["messages"]:
        role = msg["role"]
        content = msg["content"]

        if role in ("user", "assistant", "tool"):
            chatml_messages.append({"role": role, "content": content})

    return {"messages": chatml_messages}

def build_training_dataset():
    """Process all logs and build training dataset."""
    all_examples = []

    for log_file in sorted(LOG_DIR.glob("*.json")):
        with open(log_file) as f:
            session = json.load(f)

        if quality_filter(session):
            example = convert_to_chatml(session)
            all_examples.append(example)

    print(f"Filtered {len(all_examples)} quality sessions from {len(list(LOG_DIR.glob('*.json')))} total")

    # Save as HuggingFace dataset
    dataset = Dataset.from_list(all_examples)
    dataset.save_to_disk(str(OUTPUT_DIR / "bolthands-self-training"))

    # Also save as JSONL for inspection
    with open(OUTPUT_DIR / "bolthands-self-training.jsonl", "w") as f:
        for ex in all_examples:
            f.write(json.dumps(ex) + "\n")

    return dataset
```

### 6.4 Automated Re-training Pipeline

```bash
#!/bin/bash
# retrain-bolthands.sh — Run weekly via cron or manually
# Crontab: 0 3 * * 0 ~/ai-drive/bolthands/retrain-bolthands.sh

set -e

BOLTHANDS_DIR=~/ai-drive/bolthands
TRAINING_DATA=$BOLTHANDS_DIR/training-data
MODELS_DIR=~/ai-drive/ai-suite/models
UNSLOTH_VENV=~/ai-drive/ai-suite/unsloth-studio

echo "$(date): Starting BoltHands retraining..."

# Step 1: Convert new session logs to training data
source $UNSLOTH_VENV/bin/activate
python $BOLTHANDS_DIR/scripts/convert_logs_to_training.py

# Step 2: Check if we have enough new data (minimum 50 new examples)
NEW_COUNT=$(python -c "
from datasets import load_from_disk
ds = load_from_disk('$TRAINING_DATA/bolthands-self-training')
print(len(ds))
")
echo "Training examples available: $NEW_COUNT"

if [ "$NEW_COUNT" -lt 50 ]; then
    echo "Not enough new training data ($NEW_COUNT < 50). Skipping."
    exit 0
fi

# Step 3: Run fine-tuning
python $BOLTHANDS_DIR/scripts/train_bolthands.py \
    --data-dir $TRAINING_DATA/bolthands-self-training \
    --output-dir $BOLTHANDS_DIR/checkpoints/$(date +%Y%m%d) \
    --base-model unsloth/Qwen3.5-9B \
    --epochs 2 \
    --lr 1e-4

# Step 4: Export to GGUF
python $BOLTHANDS_DIR/scripts/export_gguf.py \
    --checkpoint $BOLTHANDS_DIR/checkpoints/$(date +%Y%m%d) \
    --output $MODELS_DIR/bolthands-qwen35-9b-$(date +%Y%m%d)-q5_k_m.gguf \
    --quant q5_k_m

# Step 5: Run evaluation
python $BOLTHANDS_DIR/scripts/eval_bolthands.py \
    --model $MODELS_DIR/bolthands-qwen35-9b-$(date +%Y%m%d)-q5_k_m.gguf \
    --baseline $MODELS_DIR/bolthands-qwen35-9b-current.gguf \
    --output $BOLTHANDS_DIR/eval-results/$(date +%Y%m%d).json

# Step 6: Deploy if better (check eval results)
IMPROVED=$(python -c "
import json
with open('$BOLTHANDS_DIR/eval-results/$(date +%Y%m%d).json') as f:
    results = json.load(f)
print('yes' if results['new_score'] > results['baseline_score'] else 'no')
")

if [ "$IMPROVED" = "yes" ]; then
    echo "New model is better! Deploying..."
    cp $MODELS_DIR/bolthands-qwen35-9b-$(date +%Y%m%d)-q5_k_m.gguf \
       $MODELS_DIR/bolthands-qwen35-9b-current.gguf
    # Restart llama.cpp server
    systemctl --user restart bolthands-llm
    echo "Deployed new model version $(date +%Y%m%d)"
else
    echo "New model is not better. Keeping current version."
fi

echo "$(date): Retraining pipeline complete."
```

### 6.5 Why This Works (Rejection Filtering Training)

This approach is essentially **Rejection Filtering Training (RFT)** — the same technique used to train Qwen3-Coder:

1. **Generate trajectories:** The model attempts tasks in real usage
2. **Filter for quality:** Only successful sessions become training data
3. **Fine-tune on successes:** The model learns from its own best outputs
4. **Iterate:** Each generation of the model produces better trajectories

Key insights from the Qwen3-Coder paper:
- RFT with just 491 successful trajectories gave +14% on SWE-bench
- Quality of trajectories matters more than quantity
- The model improves most on task types it encounters frequently
- This is the same principle as DeepSeek-R1's self-improvement loop

### 6.6 Safety Rails for Self-Improvement

- **Never auto-deploy without evaluation** — always compare against current model
- **Keep all previous model versions** — easy rollback
- **Monitor for reward hacking** — if the model starts "gaming" the quality filter (e.g., producing trivially correct but unhelpful responses), tighten the filter
- **Periodic human review** — sample 20 training examples monthly, check for quality drift
- **Catastrophic forgetting protection** — always mix in some original training data (10-20%) when retraining to prevent forgetting general capabilities

---

## 7. Complete Training Script

```python
"""
train_bolthands.py — Full fine-tuning script for BoltHands
Run from within the Unsloth Studio venv.
"""

from unsloth import FastLanguageModel
import torch
from datasets import load_dataset, concatenate_datasets, Dataset
from trl import SFTTrainer, SFTConfig
import json

# ============================================================
# Configuration
# ============================================================

MODEL_NAME = "unsloth/Qwen3.5-9B"    # Unsloth's optimized 4-bit version
MAX_SEQ_LENGTH = 8192
LOAD_IN_4BIT = True                   # QLoRA
OUTPUT_DIR = "bolthands-qwen35-9b-lora"
LORA_RANK = 64
LORA_ALPHA = 64
EPOCHS = 2
LEARNING_RATE = 2e-4
BATCH_SIZE = 2
GRAD_ACCUM = 8

# ============================================================
# Step 1: Load Model
# ============================================================

print("Loading model...")
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name = MODEL_NAME,
    max_seq_length = MAX_SEQ_LENGTH,
    dtype = None,
    load_in_4bit = LOAD_IN_4BIT,
)

# ============================================================
# Step 2: Apply LoRA
# ============================================================

print("Applying LoRA...")
model = FastLanguageModel.get_peft_model(
    model,
    r = LORA_RANK,
    target_modules = [
        "q_proj", "k_proj", "v_proj", "o_proj",
        "gate_proj", "up_proj", "down_proj",
    ],
    lora_alpha = LORA_ALPHA,
    lora_dropout = 0,
    bias = "none",
    use_gradient_checkpointing = "unsloth",
    random_state = 3407,
    max_seq_length = MAX_SEQ_LENGTH,
    use_rslora = True,
)

# ============================================================
# Step 3: Load & Prepare Datasets
# ============================================================

print("Loading datasets...")

# Dataset 1: OpenHands agent trajectories (filtered for success)
openhands_ds = load_dataset(
    "nebius/SWE-rebench-openhands-trajectories",
    split="train"
)
openhands_ds = openhands_ds.filter(lambda x: x.get("resolved", False))
print(f"  OpenHands successful trajectories: {len(openhands_ds)}")

# Dataset 2: Function calling examples
hermes_ds = load_dataset(
    "NousResearch/hermes-function-calling-v1",
    split="train"
)
print(f"  Hermes function calling: {len(hermes_ds)}")

# Dataset 3: Custom BoltHands behavioral examples
bolthands_custom = load_dataset(
    "json",
    data_files="~/ai-drive/bolthands/training-data/bolthands-custom.jsonl",
    split="train"
)
print(f"  Custom BoltHands examples: {len(bolthands_custom)}")

# ============================================================
# Step 4: Format all datasets to ChatML
# ============================================================

SYSTEM_PROMPT = """You are BoltHands, an expert agentic coding assistant. You solve programming tasks by reading files, running commands, and iterating on errors.

CORE BEHAVIORS:
- Always read files before modifying them
- Save research and plans to files before starting complex work
- When errors occur, search for the error message to understand the cause
- Write a PLAN.md before starting multi-step tasks
- Test changes after making them
- Never give up after the first error — iterate until resolved

<tools>
[{"type": "function", "function": {"name": "bash", "description": "Execute a bash command", "parameters": {"type": "object", "properties": {"command": {"type": "string"}}, "required": ["command"]}}},
{"type": "function", "function": {"name": "write_file", "description": "Write content to a file", "parameters": {"type": "object", "properties": {"path": {"type": "string"}, "content": {"type": "string"}}, "required": ["path", "content"]}}},
{"type": "function", "function": {"name": "read_file", "description": "Read a file", "parameters": {"type": "object", "properties": {"path": {"type": "string"}}, "required": ["path"]}}},
{"type": "function", "function": {"name": "search_files", "description": "Search for a pattern in files", "parameters": {"type": "object", "properties": {"pattern": {"type": "string"}, "path": {"type": "string"}}, "required": ["pattern"]}}}]
</tools>

When you need to use a tool, respond with:
<tool_call>
{"name": "tool_name", "arguments": {"arg1": "value1"}}
</tool_call>"""

def format_to_text(example):
    """Format a conversation to a single text string for SFT."""
    messages = example.get("messages") or example.get("conversations", [])
    text = ""

    # Add system prompt if not present
    if messages and messages[0].get("role", messages[0].get("from", "")) != "system":
        text += f"<|im_start|>system\n{SYSTEM_PROMPT}\n<|im_end|>\n"

    for msg in messages:
        role = msg.get("role", msg.get("from", "user"))
        content = msg.get("content", msg.get("value", ""))

        # Normalize role names
        if role in ("gpt", "model", "assistant"):
            role = "assistant"
        elif role in ("human",):
            role = "user"

        text += f"<|im_start|>{role}\n{content}\n<|im_end|>\n"

    return {"text": text}

# Apply formatting to all datasets
# (You'll need to adapt the OpenHands converter from Section 3.1)
bolthands_custom = bolthands_custom.map(format_to_text)
hermes_ds = hermes_ds.map(format_to_text)

# Combine datasets
# Take a balanced mix: all custom, subset of others
combined = concatenate_datasets([
    bolthands_custom,
    hermes_ds.shuffle(seed=42).select(range(min(10000, len(hermes_ds)))),
    # Add formatted OpenHands data here
])
combined = combined.shuffle(seed=42)

# Train/eval split
split = combined.train_test_split(test_size=0.05, seed=42)
train_ds = split["train"]
eval_ds = split["test"]
print(f"\nTotal training examples: {len(train_ds)}")
print(f"Eval examples: {len(eval_ds)}")

# ============================================================
# Step 5: Train
# ============================================================

print("\nStarting training...")
trainer = SFTTrainer(
    model = model,
    tokenizer = tokenizer,
    train_dataset = train_ds,
    eval_dataset = eval_ds,
    args = SFTConfig(
        per_device_train_batch_size = BATCH_SIZE,
        gradient_accumulation_steps = GRAD_ACCUM,
        num_train_epochs = EPOCHS,
        learning_rate = LEARNING_RATE,
        lr_scheduler_type = "cosine",
        warmup_ratio = 0.05,
        fp16 = not torch.cuda.is_bf16_supported(),
        bf16 = torch.cuda.is_bf16_supported(),
        optim = "adamw_8bit",
        max_seq_length = MAX_SEQ_LENGTH,
        dataset_text_field = "text",
        logging_steps = 10,
        save_strategy = "steps",
        save_steps = 500,
        save_total_limit = 3,
        eval_strategy = "steps",
        eval_steps = 500,
        output_dir = OUTPUT_DIR,
        seed = 3407,
    ),
)

trainer_stats = trainer.train()
print(f"\nTraining complete! Stats: {trainer_stats}")

# ============================================================
# Step 6: Export
# ============================================================

print("\nExporting to GGUF...")

# Save LoRA adapter (for stacking)
model.save_pretrained(f"{OUTPUT_DIR}/lora-adapter")
tokenizer.save_pretrained(f"{OUTPUT_DIR}/lora-adapter")

# Save merged GGUF (for direct deployment)
model.save_pretrained_gguf(
    f"{OUTPUT_DIR}/gguf-q5_k_m",
    tokenizer,
    quantization_method = "q5_k_m"
)

model.save_pretrained_gguf(
    f"{OUTPUT_DIR}/gguf-q4_k_m",
    tokenizer,
    quantization_method = "q4_k_m"
)

print("Done! Models saved to:")
print(f"  LoRA adapter: {OUTPUT_DIR}/lora-adapter/")
print(f"  GGUF (q5_k_m): {OUTPUT_DIR}/gguf-q5_k_m/")
print(f"  GGUF (q4_k_m): {OUTPUT_DIR}/gguf-q4_k_m/")
```

---

## 8. Quick-Start Checklist

1. [ ] **Download datasets** from HuggingFace (OpenHands trajectories, Hermes function calling)
2. [ ] **Generate 500+ custom examples** using Claude API for BoltHands-specific behaviors
3. [ ] **Format all data** to Qwen3.5 ChatML with tool-calling XML tags
4. [ ] **Activate Unsloth Studio venv** at `~/ai-drive/ai-suite/unsloth-studio/`
5. [ ] **Run training script** — expect ~6-12 hours for 30K examples on RTX 3090
6. [ ] **Export to GGUF** (q5_k_m recommended)
7. [ ] **Run BFCL eval** + custom BoltHands eval suite
8. [ ] **A/B test** against base Qwen3.5-9B
9. [ ] **Deploy** to llama.cpp router
10. [ ] **Set up session logging** for the self-improvement loop
11. [ ] **Schedule weekly retraining** via cron

---

## Sources

### Unsloth Documentation
- [Qwen3.5 Fine-tuning Guide](https://unsloth.ai/docs/models/qwen3.5/fine-tune)
- [Qwen3.5 How to Run Locally](https://unsloth.ai/docs/models/qwen3.5)
- [LoRA Hyperparameters Guide](https://unsloth.ai/docs/get-started/fine-tuning-llms-guide/lora-hyperparameters-guide)
- [Saving to GGUF](https://unsloth.ai/docs/basics/inference-and-deployment/saving-to-gguf)
- [Unsloth Data Recipes](https://unsloth.ai/docs/new/studio/data-recipe)
- [Datasets Guide](https://unsloth.ai/docs/get-started/fine-tuning-llms-guide/datasets-guide)

### Datasets
- [nebius/SWE-rebench-openhands-trajectories](https://huggingface.co/datasets/nebius/SWE-rebench-openhands-trajectories) — 67K agent trajectories (CC-BY-4.0)
- [NousResearch/hermes-function-calling-v1](https://huggingface.co/datasets/NousResearch/hermes-function-calling-v1) — Function calling + structured output
- [Salesforce/xlam-function-calling-60k](https://huggingface.co/datasets/Salesforce/xlam-function-calling-60k) — 60K verified function-calling examples
- [glaiveai/glaive-function-calling-v2](https://huggingface.co/datasets/glaiveai/glaive-function-calling-v2) — 113K function calling examples (Apache 2.0)
- [hypervariance/function-calling-sharegpt](https://huggingface.co/datasets/hypervariance/function-calling-sharegpt) — 87K ShareGPT format
- [gorilla-llm/Berkeley-Function-Calling-Leaderboard](https://huggingface.co/datasets/gorilla-llm/Berkeley-Function-Calling-Leaderboard) — BFCL eval data (Apache 2.0)

### Research Papers & Projects
- [SWE-Gym (ICML 2025)](https://arxiv.org/abs/2412.21139) — Training Software Engineering Agents
- [Qwen3-Coder Technical Report](https://arxiv.org/pdf/2603.00729) — Agentic coding with RFT
- [OpenHands Trajectories with Qwen3-Coder](https://nebius.com/blog/posts/openhands-trajectories-with-qwen3-coder-480b)
- [ToolBench (ICLR 2024)](https://github.com/OpenBMB/ToolBench) — Tool learning platform
- [Gorilla: LLM Connected with APIs](https://github.com/ShishirPatil/gorilla)
- [Agent Data Protocol](https://arxiv.org/html/2510.24702) — Unifying agent trajectory formats
- [xLAM: Large Action Models](https://github.com/SalesforceAIResearch/xLAM)

### llama.cpp LoRA
- [LoRA hot-swap endpoint](https://github.com/ggml-org/llama.cpp/discussions/7850)
- [Per-request LoRA adapters](https://github.com/ggml-org/llama.cpp/issues/10377)
- [Multiple LoRA support in llama-cpp-python](https://github.com/abetlen/llama-cpp-python/pull/1817)

### Tutorials
- [QLoRA Fine-Tuning on Consumer GPU (Unsloth)](https://www.stack-junkie.com/blog/lora-fine-tuning-consumer-gpu)
- [Building a QLoRA Pipeline with Unsloth (MarkTechPost, 2026)](https://www.marktechpost.com/2026/03/03/how-to-build-a-stable-and-efficient-qlora-fine-tuning-pipeline-using-unsloth-for-large-language-models/)
- [Fine-Tune LLM Agent for Tool Use (HuggingFace)](https://kyrylai.com/2025/04/01/fine-tune-llm-agent-tool-use-huggingface/)
- [Fine-Tuning Open Source Models for Function Calling with Unsloth](https://towardsai.net/p/machine-learning/fine-tuning-open-source-models-for-function-calling-a-complete-guide-with-unsloth-and-docker)
- [Qwen3 Chat Template Deep Dive](https://huggingface.co/blog/qwen-3-chat-template-deep-dive)
